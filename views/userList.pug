extends layout

block content
  .inner
    h1 users
    p I'm #{user.name}
    if users
      ul
        each user in users
          li
            span= user.name
              form(action="/chat" method="post")
                input(type="hidden", name="userId" value=user.id)
                button.button(type="submit") start chat